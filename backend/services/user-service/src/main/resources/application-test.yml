spring:
  datasource:
    url: jdbc:mysql://localhost:3306/apptest_db
    username: exampleuser
    password: examplepass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
